# 腾讯云部署

## 推荐方式：COS 静态网站托管

这个作品集是静态 HTML/CSS/JavaScript 网站，不需要运行 Node 服务即可部署到腾讯云 COS。

### 1. 创建存储桶

1. 登录腾讯云控制台，进入对象存储 COS。
2. 创建存储桶，地域选择距离访问用户较近的地域。
3. 访问权限选择“公有读私有写”。
4. 开启“静态网站”功能，将首页设置为 `index.html`。

### 2. 上传文件

上传以下内容，并保持目录结构不变：

- `index.html`
- `styles.css`
- `script.js`
- `assets/`

不需要上传 `node_modules/`、`server.js` 和 `package-lock.json`。

### 3. 绑定域名

在 COS 的“域名与传输管理”中绑定自己的域名，并配置 DNS CNAME。

正式在国内微信中长期发送，建议使用已完成 ICP 备案的域名。未备案域名可能无法稳定访问或无法绑定国内节点。

### 4. 开启 HTTPS

在腾讯云 SSL 证书服务申请或上传证书，然后为绑定域名开启 HTTPS。微信中发送的正式链接应使用：

`https://你的域名/`

### 5. 视频说明

当前作品视频总体积约 1.13GB，其中一个地产视频约 633MB。建议先保留现状验证页面，再把大视频压缩成 1080p H.264 版本，以降低首次播放和流量成本。

## 注意

直接使用 COS 默认域名可以先测试，但不建议作为微信作品集正式链接。正式发送建议使用已备案域名、HTTPS 和 CDN。
